spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3366/dbman
    username: root
    password: hxl2580
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

